# Python API. Copy to .env and run uvicorn with --env-file .env.
# Disabled until route content, representative coordinates and licensing are reviewed.
CONDITIONS_ENABLED=false
OPEN_METEO_API_KEY=
# For private non-commercial evaluation only, never a commercial licensing bypass.
WEATHER_ALLOW_NONCOMMERCIAL=false
